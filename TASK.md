# TASK.md — CrowdTest Fix Round: Trust + UX Issues

## ⚠️ DO NOT MERGE ANY PRs. Create PR and STOP.

## Overview
Fix the top issues found by CrowdTest (2026-03-23, score 7.0/10).
Target: push score to 8+/10, NPS 7+. Create ONE PR with all changes.

## 1. Remove fake charts from Scorecard (CRITICAL — Trust)
- File: `packages/web/src/pages/Scorecard.tsx`
- **RollingAccuracyTrend** component uses `Math.sin()` to generate fake trend data. This is DECEPTIVE.
  - Replace with an honest "Coming soon" placeholder card:
  - Show: "Rolling accuracy trend — Coming soon. We're collecting enough data to show meaningful trends. Check back in a few weeks."
  - Use a subtle dashed-border card with a chart icon, not a fake chart.
- **SeverityBreakdownChart** uses eventType bucket positions as a proxy for severity distribution. This is MISLEADING.
  - The backend has REAL severity data in the events table. Add a new API endpoint or query:
  - `GET /api/v1/scorecards/severity-breakdown?days=N` → returns `{severity: string, count: number}[]`
  - Backend query: `SELECT severity, COUNT(*) as count FROM events WHERE created_at > NOW() - INTERVAL 'N days' GROUP BY severity ORDER BY CASE severity WHEN 'CRITICAL' THEN 1 WHEN 'HIGH' THEN 2 WHEN 'MEDIUM' THEN 3 WHEN 'LOW' THEN 4 END`
  - Frontend: fetch real data and display it in the donut chart
  - If API fails or returns empty: show "No severity data available yet"

## 2. Add 404 catch-all route (UX-Critical)
- File: `packages/web/src/App.tsx`
- Add a catch-all route `{ path: '*', element: <NotFound /> }` as last child of the AppShell route
- Create `packages/web/src/pages/NotFound.tsx`:
  - Show: "Page not found" heading, "The page you're looking for doesn't exist." description
  - "Go to Feed →" link button back to `/`
  - Use consistent styling with the app (dark theme, same fonts)
  - Keep it simple and clean

## 3. Add tooltips on jargon terms (Accessibility)
- Add title attributes or small tooltip popovers on key jargon:
  - **Severity badges** (in feed cards and detail): "CRITICAL = Major market-moving event, HIGH = Significant event, MEDIUM = Notable event, LOW = Minor event"
  - **Direction badges** (bullish/bearish): "Bullish = Expected to push price UP, Bearish = Expected to push price DOWN"
  - File: `packages/web/src/components/SeverityBadge.tsx` — add `title` attribute with explanation
  - File: `packages/web/src/components/DirectionBadge.tsx` — add `title` attribute with explanation
- **Scorecard metrics**: Add small "?" icon with tooltip next to jargon terms:
  - "Directional hit rate" → "How often the predicted direction (up/down) matched actual price movement"
  - "T+20 move" → "Price change 20 trading days (~1 month) after the event"
  - "Setup worked" → "How often the event led to a tradeable move of 5%+"
  - File: `packages/web/src/pages/Scorecard.tsx` — add info icons with title tooltips next to metric labels

## 4. Flesh out About page (Trust)
- File: `packages/web/src/pages/About.tsx`
- Expand from one paragraph to a proper About page:
  - **What is Event Radar?** — 2-3 paragraphs explaining the product
  - **Data Sources** — List all active sources: SEC EDGAR, StockTwits, Reddit, Breaking News, Truth Social, X/Twitter, Federal Register, Trading Halts, Economic Calendar, IR Monitor, Newswire, Whitehouse, FTC, FDA, Dilution Monitor
  - **How It Works** — Brief pipeline explanation: Sources → AI Classification → Golden Judge → Delivery
  - **AI Disclosure** — "Event summaries, bull/bear cases, and severity classifications are generated by AI (GPT-4). Always verify with primary sources."
  - **Contact** — "Questions? Reach us at [placeholder email]"
  - Keep the professional dark-theme styling

## 5. Add "AI-generated" label on analysis (Transparency)
- File: `packages/web/src/pages/EventDetail/EventEnrichment.tsx`
- Add a small subtle label at the top of the Summary tab:
  - "🤖 AI-generated analysis · Verify with primary sources"
  - Style: text-xs text-text-secondary, slightly muted
- Also add to bull/bear case sections if they exist

## Testing Requirements
- `pnpm --filter @event-radar/web test` — all tests must pass
- `pnpm --filter @event-radar/backend test` — all tests must pass (if backend changed)
- `pnpm --filter @event-radar/web build` — must succeed
- Add test for NotFound page rendering
- Add test for severity breakdown API endpoint (if created)

## PR
- Branch: `feat/phase4-crowdtest-fixes`
- Title: `feat: CrowdTest fixes — fake charts, 404, tooltips, about page, AI labels`
- **DO NOT MERGE. Create PR and stop.**
