import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tailwind CSS v4 is configured via CSS @import in src/app/globals.css
  // No additional config needed for Tailwind v4 with Next.js 15
};

export default nextConfig;
