/**
 * Econ calendar utility functions.
 * Re-exported from the scanner for backward compatibility.
 */
export {
  isPreAlertWindow,
  isPostRelease,
  parseFeedEvents,
  type FeedEvent,
  type ParsedRelease,
} from '../scanners/econ-calendar-scanner.js';
